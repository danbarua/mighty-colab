---
log:
2026-05-07: Fixed `colab console` piped-stdin handling. Previously a piped invocation (e.g. `echo 'cmd' |mighty-colabconsole -s s`) sent the command and then hung indefinitely because the previous EOF handler emitted a bare `\x04` (Ctrl-D), which the remote `tmux`-wrapped bash treats as a literal character rather than a session terminator. The new handler sends `exit\n` (which bash actually exits on) and then closes the websocket from the client side after a short grace period (`PIPED_EOF_GRACE_SECONDS = 0.5s`) so any tail output (bash `logout`, tmux `[exited]`) makes it back to the user. TTY mode is unchanged: real-terminal EOF is left to the remote shell. Verified live: `echo 'echo HELLO' |mighty-colabconsole -s s` now exits in ~1.2s instead of hanging.

2026-05-07: Fixed `print_kitty` (used by `colab exec --output-image` and any image-producing exec) to no-op when `sys.stdout.isatty()` is false. The Kitty Graphics Protocol escape sequence is meaningless when stdout is a file or pipe and was visually corrupting captured output (a multi-KB base64 PNG blob would land in log files, grep targets, or showboat captures). Image bytes are still saved to disk via `handle_image`'s file-write path; only the inline-render attempt is suppressed.

2026-06-04: Bumped the default `--timeout` for `colab exec` from 10s to 30s (and the matching `colab run` default) so brief silent tasks are less likely to hit a premature `TimeoutError`. Explicit `--timeout` overrides are unaffected.

2026-08-11: Added `colab exec-async`, the background counterpart to `colab exec` (which blocks the caller until the run finishes). It re-spawns the real `exec` command as a detached process, redirecting stdout/stderr to a log file instead of the caller's terminal, and returns immediately; `SessionState.exec_pid`/`exec_log_path` track the job. `colab log -s <session> -f` tails that file live. A second `exec-async` on the same session is refused while one is still running (checked via `os.kill(pid, 0)`, so a stale pid never blocks a restart); a cross-session guard also refuses if another live session's log path would collide. `--output-log <path>` lets the caller (typically a sandboxed/autonomous agent, rather than a human who gets live streaming from plain `exec`) redirect that log anywhere writable, creating the parent directory if needed; each run overwrites rather than appends, matching the default path's existing behavior. `colab status` prints `Log: <path>` whenever one is tracked. `colab stop` also kills a live `exec_pid`. Also added `colab log -s <session> --tail [-n N]`, a non-blocking sibling to `-f`: prints the log's current content once and exits, no polling or liveness check, whether the job is running or finished. Built for MCP callers specifically -- `-f` blocks a single MCP tool call for the job's entire, potentially unbounded duration and only returns output once, at the very end, which Claude Desktop's request/response tool-call model can't represent; `--tail` is exposed automatically with no `EXCLUDED_PARAMS` entry needed, since it's just a bounded synchronous file read. Verified live against a real Colab session end to end, including a mid-run `--tail` returning in under a second with partial output. Also fixed a real reported incident (`docs/AGENT_USABILITY_LEARNINGS.md`, "the driver that crashed on a billing A100"): `exec -f` transmits a file's *text* into an existing kernel rather than running it as a script, so `sys.argv`/`__name__`/`__file__` never existed -- module-scope code referencing `__file__` (e.g. to locate a sibling module) raised `NameError`. `exec -f` (non-notebook only) and `run` (which already set `sys.argv`/`__name__` but not `__file__`) now share one prelude builder (`_build_script_prelude`, moved out of `run.py` into `execution.py`) giving both `sys.argv = [basename]`, `__name__ = '__main__'`, and a synthetic `__file__ = '<mighty-colab-exec:basename>'` sentinel -- not the caller's real local path, which would be more misleading, not less, since nothing from the local filesystem exists on the VM. Piped/stdin `exec` is deliberately unaffected (no filename to honor). Verified live: the exact `os.path.dirname(os.path.abspath(__file__))` pattern from the original incident no longer crashes, under both `exec -f` and `run`.

2026-08-12: Added a global `--json` flag for `exec`/`run`/`exec-async`/`log --tail`, addressing `docs/AGENT_USABILITY_LEARNINGS.md` asks #2/#6/#8. Designed against real feedback from the consumer agent behind those asks (a real incident: a script that succeeded then hit `raise SystemExit(0)`, which IPython reports as an `error`-type kernel output, made `exec` return rc=1 and a Makefile recipe tore down the session with its only result still on it), plus a second review pass. Key design split: under `--json` the CLI process's own exit code stays 0 whenever it mechanically completed its transaction -- even if the remote job raised -- with the job's own outcome carried separately in the envelope as `status` (`"ok"`/`"job_raised"`/`"error"`) + `exit_code` + `reason`; `SystemExit(0)`/`None`/`False` always normalizes to `status="ok"`, never `"job_raised"`, derived via the same CPython-mirroring helper (`common._exit_code_from_outputs`, relocated out of `run.py` so `exec` and `run` share it) rather than a naive error-type check. `exec`/`run` reuse the `outputs` `runtime.execute_code` already returns (nbformat-shaped; `exec`'s `blocks` list, `run`'s single `outputs` list), suppressing the human-display `output_hook` under `--json` so raw kernel stdout/ANSI never leaks into the stdout JSON stream; tracebacks are ANSI-stripped by default with the raw original preserved as `traceback_raw`. Every envelope carries `schema_version`/`cli_version` (a real incident that night was a stale venv running an older CLI than the shell's `--help` showed). All `[colab] ...` chatter is redirected to stderr under `--json` via a single `typer.echo` monkey-patch installed at `cli.py` module load (every call site in the codebase is module-qualified, so one reassignment covers all of them) -- `--json` also implies `--logtostderr` as an unrelated, free complementary flip (`logging` module output vs. `typer.echo`). `exec-async --json` returns `{status:"started", pid, log_path}` immediately, unchanged non-blocking contract; its spawned child gets a new hidden `--json-result-path` flag (never `--json` itself, which would suppress its `output_hook` and break `--tail`'s live rendering) that writes the same envelope `exec --json` would print to stdout, to a `<log_path>.json` sidecar file instead, once the run finishes -- a durable file rather than CLI-held state, so the result survives the session being stopped. `log --tail --json` is sidecar-aware (three states: sidecar present and parses -> return its fields as-is; no sidecar, pid alive -> `status="running"`; no sidecar, pid dead -> `status="error", reason="worker_terminated"`, an intentionally generic code since mighty-colab has no visibility into *why* an external actor ended the worker) and gains `--since-offset N` / `next_offset` for incremental polling instead of re-reading the whole log every tick; since the sidecar path is derivable from the session name alone, it still resolves after `stop` removes the session record (custom `--output-log` paths are the one documented gap -- only resolvable while the session record exists). Unit-tested extensively (`tests/test_common_json.py`, `test_json_flag.py`, `test_exec_json.py`, `test_run_json.py`, `test_exec_async_json.py`, `test_log_tail_json.py`); a live integration script (`integration/repro_json_output/`) covering the `SystemExit(0)` case and the sidecar-survives-`stop` case end to end was written but not yet run against a real backend as of this entry. Deferred to a follow-up: Click-level parse-error JSON shaping, and `--json`/reason codes for `new`/`adopt` (session provisioning) and `files.py`.
---

# Design: Execution and Interactive Interaction (`repl`, `exec`, `console`)

## Overview
Execution involves sending Python code (or shell commands) to the Jupyter kernel running on the Colab VM and processing the stream of output messages.

## Approach

### 1. REPL (`colab repl`)
- **Transport**: WebSockets (using `websockets` library if allowed, or a custom `http.client` based long-polling implementation if we're strictly stdlib).
- **Communication**: Jupyter Kernel Messaging Protocol.
    - `execute_request`: Send code string.
    - `execute_reply`: Get status.
    - `iopub.stream`: Capture `stdout` and `stderr`.
- **Interactive Mode**: Standard Python `cmd.Cmd` or `code.InteractiveConsole` for local input/output.
- **Piping Support**: Detect `sys.stdin.isatty()`. If not a TTY, read all input and send as a single execution request.

### 2. Execution (`colab exec`)
- **File Handling**:
    - If file path is local: Read content, send as code.
    - If file path is remote: Execute `!python <path>`.
- **Multi-Modal Output**: Handle `display_data` messages (e.g., `image/png`, `text/html`). For the CLI, we'll save images to temporary files and print their paths, or if the terminal supports it (e.g., iTerm2), inline them.
- **Timeout Configuration**: Exposes a `--timeout` flag (default 30s) to allow long-running silent tasks (like model compilation or data downloading) to execute without being prematurely killed.
- **Script prelude (`-f`, non-notebook only)**: `_build_script_prelude()` (`execution.py`, shared with `run.py`) prepends `sys.argv = [basename]`, `__name__ = '__main__'`, and `__file__ = '<mighty-colab-exec:basename>'` before the file's own text. Since the code is transmitted as text into an existing kernel rather than run as a script, none of these exist by default -- module-scope code assuming any of them (e.g. `os.path.dirname(os.path.abspath(__file__))` to locate a sibling module) raises `NameError`, which crashed a real billing run. `__file__` is a synthetic sentinel, not the caller's real local path -- a real-looking path would be actively misleading (`open(__file__)` failing on a path that looks like it should exist, instead of obviously not) for a file that doesn't exist on the VM at all. Deliberately scoped to `-f` only (piped/stdin code has no filename to honor `sys.argv`/`__file__` with, so it's sent unmodified) and to non-notebook files (notebook cells have their own per-cell identity, not a single script's). Re-applied on every `exec -f` call even though the kernel's namespace persists across calls in a session -- each call means "treat this text as if it were `python script.py`," so the values should reflect *that* call every time, not whatever a previous one set.

### 2b. Background Execution (`colab exec-async` / `colab log -f`)
- **Motivation**: `colab exec` blocks the caller for the entire run. For a long training job the caller either has to keep a terminal tied up, or -- if they instead poll `colab log` (no `-f`) -- see nothing, since the JSONL history's `execution` event is only written once the whole call returns.
- **Implementation**: `exec-async` does not introduce a new execution path. It spawns the *actual* `exec` command as a detached daemon (same `subprocess.Popen(..., start_new_session=True)` pattern `spawn_keep_alive` uses for keep-alive), with stdout/stderr redirected to a raw log file instead of the caller's terminal. This means the daemon gets `exec`'s full behavior for free -- `s.running`, `s.last_execution`, the `execution` history event, notebook cell splitting, `_output.ipynb` saving, the `sys.argv`/`__name__`/`__file__` script prelude -- with zero duplicated logic.
- **State**: `SessionState.exec_pid` / `exec_log_path` track the daemon. A second `exec-async` on the same session is refused while the tracked PID is still alive (`os.kill(pid, 0)`); once it exits, a new one is allowed. `colab stop` kills a live `exec_pid` alongside `keep_alive_pid`.
- **Output redirection**: `--output-log <path>` overrides the default log path -- for a sandboxed/autonomous agent without write access to `~/.config`, typically -- creating the parent directory if needed; each run truncates rather than appends, same as the default path. `colab status` prints it as `Log: <path>` whenever tracked. Since two different sessions should never legitimately compute the same log path but `spawn_exec_async` truncates unconditionally, a guard scans other sessions before spawning and refuses if a *live* one already claims the exact path about to be used.
- **Piped stdin**: since a detached child's stdin is `DEVNULL`, piped code can't be forwarded live -- it's read once by the parent and materialized to a temp `.py` file under `~/.config/colab-cli/exec-async/`, then handed to the daemon via `-f`.
- **Following output**: `colab log -s <session> -f` tails the raw log file (polling + liveness check), independent of the JSONL history view the same command renders without `-f`.
- **Non-blocking peek**: `colab log -s <session> --tail [-n N]` prints the log's current content once and exits -- no polling, no liveness check at all, works identically whether the job is still running or already finished. Added specifically so MCP clients can check in on a background job: `--follow` blocks a single MCP tool call for the job's entire (potentially unbounded) duration and only returns output once, at the very end, which Claude Desktop's plain request/response tool-call model can't represent; `--tail` is a bounded synchronous file read, so it needs no `EXCLUDED_PARAMS` entry and is auto-exposed like any other safe parameter (see `docs/07_mcp_server.md`).
- **`--timeout` still fully applies to the run -- keep-alive does not substitute for it**: `exec_async()`'s own submission call never touches the kernel (no `runtime.execute_code`, no `/content` pre-flight), so *it* needs no timeout of its own. But the `--timeout` value it accepts is forwarded unchanged to the detached child's real `exec --timeout <value>`, which enforces it fully: `execute_interactive` gives up if the kernel goes quiet (no IOPub traffic) for that long, default 30s, *even though the execution may still be perfectly healthy* (see the comment on `execute_code` in `runtime.py`). Session keep-alive (`spawn_keep_alive`, a periodic HTTP ping to `.../keep-alive/`) is a completely different mechanism at a different layer -- it stops Colab's infrastructure from reclaiming an *idle VM*; it has no connection to the kernel websocket and does nothing to extend this quiet-time budget. A background job that prints nothing for longer than `--timeout` (e.g. GPU compute with no periodic logging) still aborts mid-run even with keep-alive happily running, so long-silent `exec-async` jobs should pass a generous `--timeout` explicitly rather than relying on the 30s default meant for foreground, human-watched runs.
- **`log -f` needs no `--timeout`**: unlike the run itself, its loop is driven by `pid_alive()`, not a kernel reply -- it terminates the instant the worker process exits, so there's no "waiting on something that might never come" failure mode to bound.

### 3. Console (`colab console`)
- **Implementation**: Connects directly to the backend terminal endpoint (`/colab/tty`) via WebSockets using `websocket-client`.
- **Interactive**: Bypasses the Jupyter kernel entirely to provide a raw, PTY-backed bash session on the Colab VM.
- **Terminal Management**: Configures `sys.stdin` to raw mode using `termios` and `tty`, passing single characters to the socket and writing raw ANSI escape sequences directly to `sys.stdout.buffer`. Hooks into `SIGWINCH` to communicate local terminal dimensions (`cols`/`rows`) to the remote bash environment so output rendering works perfectly during resizing.
- **Piped stdin**: Detected via `sys.stdin.isatty()`. When piped, the input characters are forwarded one at a time to the remote pty, and on EOF the client sends `exit\n` and then closes the websocket itself after `PIPED_EOF_GRACE_SECONDS` (0.5s) so the user's shell goodbye text drains back. The remote `/colab/tty` endpoint wraps bash in tmux, which intercepts a bare `\x04` as a literal character — that is why we send `exit\n` rather than Ctrl-D.

## Implementation Details
- **Kernel Management**: `ColabRuntime` (from `colab-agent`) already handles message signing and message types.
- **Output Streaming**: Continuous polling or asynchronous message handling to provide real-time output.
- **Piping Example**: `cat script.py |mighty-colabexec -s my-session`.

## Testing Strategy
TDD is mandatory for all execution features.

### 1. Mock Kernel Client
- **Test Case**: Verify `ColabRuntime` correctly sends an `execute_request` message over the websocket.
- **Test Case**: Verify `iopub.stream` messages are correctly handled and printed to `stdout` in real-time.
- **Test Case**: Verify `display_data` (specifically `image/png`) triggers the correct local handling (saving or display).

### 2. TTY and Piping
- **Test Case**: Mock `sys.stdin.isatty()` to verify `colab repl` correctly switches between interactive mode and one-shot piped execution.
- **Test Case**: Verify large piped inputs are handled without buffer overflow or truncation.
- **Test Case**: `colab console` with piped stdin sends `exit\n` and calls `ws.close()` on EOF (regression: previously sent `\x04` only and hung).
- **Test Case**: `colab console` in TTY mode does not synthesize an exit on EOF (the user owns the session lifecycle).
- **Test Case**: `print_kitty` is a no-op when `sys.stdout.isatty()` is false (regression: previously emitted ANSI/base64 into pipes and files).
